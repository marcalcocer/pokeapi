management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
metrics:
  export:
  prometheus:
    enabled: true

server:
  port: 8080

logging:
  level:
    root: INFO
    com.marcalcocer.pokemonapi: DEBUG

spring:
  redis:
    host: redis
    port: 6379
    timeout: 5000ms